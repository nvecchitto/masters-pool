<% content_for :title, "#{@pool.name} — #{@tournament.name}" %>

<%# Subscribe to live Turbo Stream updates pushed by SyncLeaderboardJob %>
<%= turbo_stream_from "pool_#{@pool.id}_standings" %>
<%= turbo_stream_from "pool_#{@pool.id}_leaderboard" %>

<div class="min-h-screen bg-gray-950 text-gray-100">

  <%# ── Header ─────────────────────────────────────────────────────────── %>
  <header class="bg-green-900 border-b border-green-700 px-6 py-4 flex items-center justify-between">
    <div>
      <h1 class="text-2xl font-bold tracking-tight text-white"><%= @tournament.name %></h1>
      <p class="text-sm text-green-300 mt-0.5"><%= @pool.name %></p>
    </div>
    <span class="<%= tournament_status_classes(@tournament) %> px-3 py-1 rounded-full text-xs font-semibold uppercase tracking-wide">
      <%= @tournament.status.humanize %>
    </span>
  </header>

  <%# ── Two-column body ─────────────────────────────────────────────────── %>
  <div class="flex flex-col lg:flex-row gap-0 h-[calc(100vh-73px)]">

    <%# ── LEFT: Team Standings (40%) ──────────────────────────────────── %>
    <aside class="lg:w-2/5 border-r border-gray-800 overflow-y-auto">
      <div class="px-5 py-4 border-b border-gray-800 flex items-center justify-between">
        <h2 class="text-lg font-semibold text-white">Pool Standings</h2>
        <span class="text-xs text-gray-400">Score includes +4/round penalty for cuts</span>
      </div>

      <div id="standings">
        <%= render "standings", teams: @teams %>
      </div>
    </aside>

    <%# ── RIGHT: Full Leaderboard (60%) ───────────────────────────────── %>
    <main class="lg:w-3/5 overflow-y-auto">
      <div class="px-5 py-4 border-b border-gray-800 flex items-center justify-between">
        <h2 class="text-lg font-semibold text-white">Field Leaderboard</h2>
        <span class="text-xs text-gray-400">
          <%= @golfers.count %> players &middot;
          <%= @drafted_ids.size %> drafted
        </span>
      </div>

      <div id="leaderboard">
        <%= render "leaderboard",
                   golfers:     @golfers,
                   pool:        @pool,
                   drafted_ids: @drafted_ids %>
      </div>
    </main>

  </div>
</div>
